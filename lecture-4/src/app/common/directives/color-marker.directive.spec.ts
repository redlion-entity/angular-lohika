import { ColorMarkerDirective } from './color-marker.directive';

describe('ColorMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
