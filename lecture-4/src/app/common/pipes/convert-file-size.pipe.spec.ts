import { ConvertFileSizePipe } from './convert-file-size.pipe';

describe('ConvertFileSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertFileSizePipe();
    expect(pipe).toBeTruthy();
  });
});
